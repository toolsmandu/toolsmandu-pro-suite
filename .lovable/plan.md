# Resize product card text

Update `src/components/ProductCard.tsx`:

- Product name `<h3>`: replace `text-base` (16px) with inline `fontSize: '12px'`
- Price `<span>`: replace `text-xl` (20px) with inline `fontSize: '12px'`, keep `font-bold` and yellow color